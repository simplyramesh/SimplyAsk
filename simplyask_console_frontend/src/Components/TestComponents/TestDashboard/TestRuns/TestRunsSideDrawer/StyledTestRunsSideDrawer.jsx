import styled from '@emotion/styled';
import { SidedrawerModal } from 'simplexiar_react_components';

export const StyledTestRunsSideDrawer = styled(SidedrawerModal)``;
export const StyledSideDrawerTitle = styled.div`
  margin-bottom: 18px;
`;

export const StyledSideDrawerFilters = styled.div`
  margin-bottom: 8px;
`;

export const StyledSideDrawerTable = styled.div``;
